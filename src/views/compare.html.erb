<%
	@isSingle = false
	@current = "compare"

	data = getCategory(@current)

	@id = data[:id]
	@title = data[:title]
	@description = data[:description]
	@tags = data[:tags]

%>
<%= render :partial => "/partials/components/section", :locals => {
			:data => data
		}
%>
