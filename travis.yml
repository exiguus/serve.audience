language: ruby
rvm:
- 1.1.5

gemfile:
- src/Gemfile

before_install:
  - gem update --system
  - gem install bundler
  - nvm install node

after_install:
  - cd src && npm install && cd ..
  - ./make-dist
